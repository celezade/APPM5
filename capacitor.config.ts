import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'appfinal11',
  webDir: 'dist/appfinal11',
  bundledWebRuntime: false
};

export default config;
